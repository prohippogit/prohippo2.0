# dependencies
node_modules/

# build output
dist/
out/
release/

# playwright browser cache (installed via postinstall)
.playwright/

# local runtime state — NEVER commit portal sessions or credentials
.state/
*.log

# Google OAuth desktop client (id + secret) — machine-local, never committed
google-oauth.json

# OS
.DS_Store
Thumbs.db

# electron-builder
*.blockmap
